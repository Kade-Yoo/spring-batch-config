spring:
  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      username: sa
      password:
      jdbc-url: jdbc:h2:mem:testdb;
  h2:
    console:
      enabled: true
  batch:
    job:
      names: ${job.name:NONE}
